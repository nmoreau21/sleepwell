/** Database `roles.id` for administrator / coordinator (combined in MVP). */
export const ADMIN_ROLE_ID = 5;

export const ADMIN_ROLE_CODE = "admin";

/** Database `roles.id` for donors. */
export const DONOR_ROLE_ID = 1;

/** Database `roles.id` for recipients. */
export const RECIPIENT_ROLE_ID = 2;
